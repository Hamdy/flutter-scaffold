enum UserType{trainee, coach}
enum DeviceType{android, ios, unknown}
enum UserTheme{dark, light, system}
enum LogLevel{info, debug, error}
enum NotificationType{achievement, msg, subscription}
enum Status{loading, success, failure}